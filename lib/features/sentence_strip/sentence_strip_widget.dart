import 'package:flutter/material.dart';

/// Sentence strip for building and displaying sentences
/// This is a placeholder for future implementation
class SentenceStripWidget extends StatelessWidget {
  const SentenceStripWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(8.0),
      color: Colors.grey[200],
      child: const Row(
        children: [
          Text('Sentence strip will be implemented here'),
        ],
      ),
    );
  }
}
